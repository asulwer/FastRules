@PACKAGE_INIT@

# FastRules uses header-only nlohmann_json and sol2
# Lua is linked statically

include(CMakeFindDependencyMacro)

# No external dependencies needed — everything is bundled

include("${CMAKE_CURRENT_LIST_DIR}/fastrules-targets.cmake")

check_required_components(fastrules)
